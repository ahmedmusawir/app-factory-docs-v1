# RIPPLE_MAP — <run name> — <date>

> Phase 2 output of the `factory-docs-update` skill. Authored by the Architect (Jarvis)
> when in the loop, else drafted by the Engineer (Claudy) with INFERENCE labels.
> Approved by the Operator at Stop Gate 2. This map — not memory, not the branch —
> is the single source of truth for Phase 4 execution.

## Entries in scope
| Entry/Lesson ID | One-line summary | Source (pack / lessons file) |
|-----------------|------------------|------------------------------|
| | | |

## Splits parked (NOT this run — other repos)
| Entry | Target repo / location | Why parked |
|-------|------------------------|------------|
| | | |

## Touch list (execute lightest ← first, heaviest ← last)
| # | Doc (canonical name) | ← count (MANIFEST) | Placement (VERIFIED vs live structure) | Change summary | New version | Rename/move? |
|---|----------------------|--------------------|----------------------------------------|----------------|-------------|--------------|
| 1 | | | | | | NO |

## New docs entering the Hub
| File | Standard header added? | MANIFEST row | Notes |
|------|------------------------|--------------|-------|
| | | | |

## Consistency ripples (docs NOT edited, but checked)
| Doc | Why checked | Verdict (CONSISTENT / NEEDS-TOUCH → move to touch list) |
|-----|-------------|--------------------------------------------------------|
| | | |

## Structural flags
- Renames/moves: <none | list — NOTE: any rename or new-doc set means local git, per standing ruling>
- Heaviest doc touched: <name, ← count> — minimal-diff rule (D14) applies hardest here
- Placement mismatches found: <none | pack said X, live doc has Y — resolution>

## Write-load tally (feeds Phase 3 estimates)
docs edited (N) + archive copies (N) + new docs (P) + MANIFEST (1) + CHANGELOG (1) [+ renames ×2] = **~K writes**

## Evidence labels
Every row above is EVIDENCE (read from MANIFEST/doc), INFERENCE (reasoned — from what?),
or QUESTION (needs the Operator). Mark non-EVIDENCE rows inline.

## Approval
- [ ] Operator APPROVED at Stop Gate 2 on: <date/time>
